baseURL = 'https://gorealstruct.com/'
languageCode = 'en-gb'
title = 'Goreal Struct'

disableKinds = ['taxonomy', 'term', 'RSS', 'sitemap']

[params]
  description = 'Integrated engineering, geospatial surveying, UAV mapping and digital construction services across UK and international infrastructure projects.'
  author = 'Goreal Struct Ltd'
  keywords = 'engineering consultancy, geospatial surveying, UAV mapping, digital construction, Trimble GNSS, infrastructure projects'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true